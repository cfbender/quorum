name = "quorum"
version = "0.2.0"
description = "Multi-model deliberation: fan out planning questions to configured member subagents, then synthesize."
entrypoint = "plugin.lua"

[capabilities]
hooks = true
commands = true
subagents = true
